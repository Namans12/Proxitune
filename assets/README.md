# ProxiTune branding

`proxitune-logo.png` is the source artwork supplied for the project. The
Android launcher sizes and the Windows multi-resolution icon are generated
from it. The Windows tray companion also loads the source artwork at runtime.
